name: Staging Deploy

on:
  push:
    branches: [staging]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Trigger Render Deploy
        run: |
          echo "Render auto deploys from staging branch!"
          echo "No action needed - Render handles staging!"
# name: Staging Deploy

# on:
#   push:
#     branches: [staging]

# jobs:
#   deploy:
#     runs-on: ubuntu-latest

#     steps:
#       - uses: actions/checkout@v3

#       - name: Deploy to EC2 via SSH
#         uses: appleboy/ssh-action@v0.1.10
#         with:
#           host: ${{ secrets.EC2_HOST }}
#           username: ubuntu
#           key: ${{ secrets.EC2_SSH_KEY }}
#           script: |
#             cd ~/Online_Food_Delivery_Web-App
#             git pull origin staging
#             npm install
#             pm2 restart all
